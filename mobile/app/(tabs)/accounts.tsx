import { Text } from 'react-native'
import { Screen } from '@/components/Screen'
import { colors } from '@/lib/theme'

export default function AccountsScreen(){return <Screen title="Accounts"><Text style={{color:colors.muted}}>Wallet cards and default account will be migrated here.</Text></Screen>}
