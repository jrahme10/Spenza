import { Text } from 'react-native'
import { Screen } from '@/components/Screen'
import { colors } from '@/lib/theme'

export default function SettingsScreen(){return <Screen title="Settings"><Text style={{color:colors.muted}}>Theme, default account, cloud sync and security will be migrated here.</Text></Screen>}
