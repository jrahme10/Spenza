import{readFileSync,writeFileSync}from'node:fs';const path=new URL('../src/App.tsx',import.meta.url);let s=readFileSync(path,'utf8');const imp=`import FamilyManager from './components/FamilyManager'`;if(!s.includes(imp)){const a=`import CloudSyncSettings from './components/CloudSyncSettings'`;s=s.replace(a,`${a}\n${imp}`)}if(!s.includes('<FamilyManager/>')){const a=`<h3 className="settingsGroupTitle">Security</h3>`;if(!s.includes(a))throw new Error('Family transform: Settings anchor missing');s=s.replace(a,`<h3 className="settingsGroupTitle">Family & Shared</h3><FamilyManager/>${a}`)}writeFileSync(path,s);console.log('Added Family and shared-space setup to Settings')