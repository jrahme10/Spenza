import{readFileSync,writeFileSync}from'node:fs';const path=new URL('../src/App.tsx',import.meta.url);let s=readFileSync(path,'utf8');const imp=`import HomeSmartInsight from './components/HomeSmartInsight'`;if(!s.includes(imp)){const a=`import ReceiptScanner from './components/ReceiptScanner'`;s=s.replace(a,`${a}\n${imp}`)}if(!s.includes('<HomeSmartInsight ')){const a=`<section className="monthCard">`;const i=s.indexOf(a);if(i<0)throw new Error('Home smart insight: month card missing');const end=s.indexOf('</section>',i)+10;s=s.slice(0,end)+`<HomeSmartInsight wallet={homeWallet} transactions={data.transactions} date={homeDate}/>`+s.slice(end)}writeFileSync(path,s);console.log('Added Home monthly budget intelligence')